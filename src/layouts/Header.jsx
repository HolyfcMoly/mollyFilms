// import { Link } from "react-router-dom";
import Navbar from "../components/Navbar";

const Header = () => (
    <header className={`w-full md:py-8 pt-4 pb-14 sticky top-0 left-0 sm:px-8 px-4 z-50 bg-[#16181b]`}>
        <Navbar />
    </header>
);

export default Header;
